fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
httpx==0.27.0
numpy==1.26.4
# shapely -- install separately: pip install shapely --only-binary :all:
python-dotenv==1.0.1
# prophet==1.1.5  -- optional, install only if needed
# pyarrow==16.0.0  -- required by prophet
